%h1 登録確認

=form_with model: [@selection, @voter], local: true do |f|
  .form-group
    = f.label :nickname, "ニックネーム"
    = f.text_field :nickname, class: "form-control"
  = f.submit '選挙登録', class: "btn btn-primary"
