
.modal-dialog
  .modal-content
    %h3 電子投票を行う場合は以下のボタンを押してください
    %h5 注意　電子投票を行う場合は市町村に設置されている投票箱での投票はできません
    =form_with model: [@selection, @voter], local: true do |f|
      .form-group
        = f.label :nickname, "ニックネーム"
        = f.text_field :nickname, class: "form-control"
      = f.submit '選挙登録', class: "btn btn-primary button_wrapper"
