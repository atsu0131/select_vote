%h3.title 選挙一覧
.mb-3
%table.table.table-hover
  %thead.thead-default
    %tr
      %th 名前
  %tbody
    - @selections.each do |selection|
      %tr
        %td= link_to "#{selection.select_name}", selection_voters_path(selection)
